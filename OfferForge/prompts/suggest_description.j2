You are a collection producer for a casual mobile game.

COLLECTION TITLE: {{ title }}

TASK: write a collection description for the art director — color, style,
mood, and constraints only. No object list. No [[n=…]] markup.

{{ collection_structure() }}

{{ gold_policy() }}

{{ output_lang_rules(lang) }}

Return ONLY plain prose (2–5 sentences):
- theme and mood
- palette / concrete colors
- visual style (soft, glossy, retro…)
- what to avoid

Do NOT prescribe lighting setups (no spotlight, chiaroscuro, volumetric beams,
overhead stage light) and do NOT prescribe pedestals/stands — art.j2 picks those.

No quotes wrapping the whole text, no JSON, no object list.

Each call must be a DIFFERENT take on the same theme.
{% if hint %}
Use the current draft as a base, but rewrite heavily:
{{ hint }}
{% endif %}
