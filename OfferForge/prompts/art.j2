{# Порядок: объект → категория композиции (ТЗ) → стиль. Референсы (если есть)
   — только look&feel; модель часто копирует предметы из Icons, это запрещено. #}
MUST DRAW THIS OBJECT (ignore any other object ideas): {{ subject }}.
{% if trigger is defined and trigger %}{{ trigger }}, {% endif %}single centered object, game card collectible icon.
{% if trigger is defined and trigger %}

STYLE LOCK ({{ trigger }} LoRA — materials & lighting only):
stylized 3D game card collectible icon, glossy materials, bold saturated colors,
rounded chunky shapes, soft studio key light from upper left,
ONE hero subject, readable silhouette, not a photograph, not cinematic still.
Staging and BACKGROUND must still follow the composition category below —
do NOT force every card into a blank floating studio void.
{% endif %}
{% if extra %}

MUST FOLLOW — producer notes for this image (do not ignore):
{{ extra }}
{% endif %}

{{ category_rule(category) }}

{{ style_rules() }}
color palette: {{ palette_words(palette) }}
avoid: {{ negative_prompt() }}, hands, fingers, photorealistic skin, text, watermark, copying reference subjects
{% if category|string != "5" %}
avoid also: people, characters, crowds
{% endif %}

SUBJECT LOCK AGAIN: the subject is "{{ subject }}" — nothing else.
{% if trigger is defined and trigger %}
If this is category 1–2, keep it icon-like and simple.
If this is category 3–4, allow the grounded surface / environment required by
the category, but keep "{{ subject }}" dominant in frame.
If this is category 5, allow one–two characters as the subject description says.
{% else %}
If reference images are attached: copy ONLY materials, lighting and stylization.
Do NOT copy objects from references (no flippers, arches, pool scenes, people,
or any other subject shown there). Replace them entirely with "{{ subject }}".
{% endif %}
{% if extra %}

Again, obey these producer notes: {{ extra }}
{% endif %}
