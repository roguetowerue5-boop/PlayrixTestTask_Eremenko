{# Positive как в ComfyUI Icons Style A (icons_flux_gguf.json / Icons_flux_comfy_guide).
   Порядок: trigger → STYLE LOCK → subject → категория → фон по категории. #}
{% if trigger is defined and trigger %}{{ trigger }}, {% endif %}{{ style_lock() }}

A glossy stylized 3D render of {{ subject }}, single hero object slightly three-quarter view, centered in frame, polished materials with bright specular hits, Playrix casual mobile-game collectible icon (keep the object's real materials/colors), soft studio lighting.

HERO OBJECT (must match): {{ subject }}.
{% if extra %}

MUST FOLLOW — producer notes for this image (do not ignore):
{{ extra }}
{% endif %}

{{ category_rule(category) }}
Staging and BACKGROUND must follow the composition category above —
do NOT force every card into a blank floating studio void unless this is category 1.

color palette: {{ palette_words(palette) }}

avoid: {{ negative_prompt() }}, photorealistic skin, copying reference subjects
{# Do NOT put bare "photograph" / "camera" in avoid — kills movie cameras & binoculars. #}
{% if category|string != "5" %}
avoid also: people, characters, crowds
{% endif %}

{% if trigger is defined and trigger %}
{% if category|string == "1" %}
Cat 1 staging (TZ): hero floats in air; monochrome gradient OR simple pattern backdrop
(stripes / repeating motif / flat color / light rays). No landscape.
{% elif category|string == "2" %}
Cat 2 staging (TZ): hero on a simple surface; behind — vertical monochrome gradient OR simple pattern
(stripes / motif / light rays). No full location.
{% elif category|string == "3" %}
Cat 3 staging (TZ): hero on a realistic surface (volumetric ground); behind — vertical monochrome
gradient OR simple pattern (stripes / light rays), not a full outdoor world.
{% elif category|string == "4" %}
Cat 4 staging (TZ): full realistic environment REQUIRED (field, sea, beach, museum, sky, stylized
mountains/hills OK) while "{{ subject }}" stays the clear central hero.
{% elif category|string == "5" %}
Cat 5 staging (TZ gold): one or two characters in a simple narrative beat; environment like cat.4.
{% endif %}
{% else %}
If reference images are attached: copy ONLY materials, lighting and stylization.
Do NOT copy objects from references. Replace them entirely with "{{ subject }}".
{% endif %}
{% if extra %}

Again, obey these producer notes: {{ extra }}
{% endif %}
