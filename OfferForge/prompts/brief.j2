You are an art director for a mobile game. Parse the producer's free-form brief into structure.

COLLECTION THEME: {{ theme }}

PRODUCER WISHES:
{{ wishes if wishes else "(none)" }}

CONTEXT: collectible cards for a casual mobile game.
{{ collection_structure() }}

{{ output_lang_rules(lang) }}

Return STRICT JSON with no commentary:
{
  "theme": "refined theme in one line (OUTPUT LANGUAGE)",
  "genre": "genre/subgenre if clear from the brief, else null",
  "mood": ["2-4 mood words (OUTPUT LANGUAGE)"],
  "palette": ["3-5 hex colors for the set palette"],
  "era": "era/period if relevant, else null",
  "must_include": ["only items the producer explicitly asked to include"],
  "must_avoid": ["only items the producer explicitly asked to exclude"],
  "notes": "one or two sentences: how you understood the task (OUTPUT LANGUAGE)"
}

Rules:
- Do not invent constraints: must_include / must_avoid only from explicit producer text.
- If a genre is named (horror, noir, western, sci-fi) — put it in genre and pick a matching palette.
- Palette should be readable at icon size (enough contrast); match the brief mood —
  do not force candy colors if the brief asks for noir / muted / dark tones.
- Do not expand must_avoid beyond what was said — global bans (text, brands, real people)
  are already in generation rules.
