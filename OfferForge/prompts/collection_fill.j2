Ты — арт-директор коллекционных наборов в мобильной игре.

НАЗВАНИЕ КОЛЛЕКЦИИ: {{ title }}

{% if brief %}
РАЗОБРАННЫЙ БРИФ (из промпта brief — опирайся на него):
{{ as_json(brief) }}
{% else %}
ОПИСАНИЕ / ПОЖЕЛАНИЯ:
{{ description }}
{% endif %}

ЗАДАЧА: предложи ровно {{ n_variants }} РАЗНЫХ вариантов наполнения страницы.
Каждый вариант — это цельный набор из {{ n_elements }} объектов-товаров для карточек.

КАК РАЗВОДИТЬ ВАРИАНТЫ (обязательно):
{{ variant_strategy() }}
- У каждого варианта свой «угол» (angle): другая подтема, эпоха, жанр или срез темы.
- Пересечение subject между любыми двумя вариантами — не больше двух.
- Не делай варианты «теми же предметами в другой палитре».

ПРАВИЛА ПОДБОРА ОБЪЕКТОВ (для КАЖДОГО варианта):
{{ collection_rules() }}
{% if brief and brief.must_include %}
- Обязательно включить хотя бы в один из вариантов: {{ brief.must_include | join(", ") }}
{% endif %}
{% if brief and brief.must_avoid %}
- Не использовать: {{ brief.must_avoid | join(", ") }}
{% endif %}

{{ gold_policy() }}

КАТЕГОРИИ КОМПОЗИЦИИ — в каждом наборе должны быть ВСЕ ЧЕТЫРЕ:
{{ category_table() }}

Распределение в каждом варианте: {{ category_distribution() }}.
Редкости в каждом варианте по порядку: {{ rarity_distribution() }}.
{% if brief and brief.palette %}
Палитру вариантов держи близко к: {{ brief.palette | join(", ") }} — но концепты разные.
{% endif %}
{% if brief and brief.mood %}
Настроение: {{ brief.mood | join(", ") }}.
{% endif %}
{% if brief and brief.genre %}
Жанр: {{ brief.genre }}.
{% endif %}

Верни строго один JSON-объект без пояснений:
{
  "variants": [
    {
      "id": "v1",
      "title": "{{ title }}",
      "angle": "коротко: чем этот вариант другой (подтема / жанр / эпоха)",
      "concept": "одно предложение: чем набор цельный",
      "palette": ["#RRGGBB", "#RRGGBB", "#RRGGBB"],
      "elements": [
        {
          "subject": "короткое английское описание объекта для генератора картинок",
          "category": "1",
          "rarity": 1,
          "title_ru": "НАЗВАНИЕ НА КАРТОЧКЕ, 1-2 слова"
        }
      ]
    }
  ]
}

В массиве variants ровно {{ n_variants }} объектов.
В elements каждого варианта ровно {{ n_elements }} объектов.

subject пиши по-английски и предметно: не «атмосфера хоррора», а «old film
projector». Это идёт прямо в генератор изображений.

ТРЕБОВАНИЯ К subject:
- Один цельный предмет, который узнаётся силуэтом. Не сцена, не композиция
  из нескольких вещей, не абстракция.
- Никаких мелких фактур: меха, сеток, ворса, россыпи мелких деталей.
- Не описывай фон, свет и тени: их задаёт категория композиции.
- Два-четыре слова.

title_ru — то, что игрок увидит на карточке: одно-два слова по-русски.
